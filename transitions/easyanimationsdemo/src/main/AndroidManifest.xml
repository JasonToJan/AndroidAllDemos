<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jan.jason.easyanimationsdemo"
    android:versionCode="1"
    android:versionName="1.0" >

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/easyAppTheme" >

        <activity
            android:name=".AnimationListActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".AnimationDetailActivity"
            android:label="@string/title_animation_detail"
            android:parentActivityName=".AnimationListActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AnimationListActivity" />
        </activity>

    </application>

</manifest>
