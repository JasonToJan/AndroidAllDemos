<resources>

    <!-- Base application theme. -->
    <style name="AppThemePopwindow" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryPopwindow</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkPopwindow</item>
        <item name="colorAccent">@color/colorAccentPopwindow</item>
    </style>

    <style name="CustomPopWindowStyle">
        <item name="android:windowEnterAnimation">@anim/popwindow_anim_in</item>
        <item name="android:windowExitAnimation">@anim/popwindow_anim_out</item>
    </style>

</resources>
